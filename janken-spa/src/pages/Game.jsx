import React from 'react';

const Game = () => (
  <div>
    <h1>Round</h1>

    <h3>Player name</h3>

    <div>
      Select move:
      <select>
        <option>test</option>
      </select>
    </div>
  </div>
);

export default Game;
